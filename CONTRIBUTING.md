# Contributing

Thank you for your interest in contributing to this project!

## Setup Development Environment

Run `./scripts/setup.sh` to set up the development environment.
This will install necessary dependencies (including Node.js packages) and download test data.

## Generate frontend API client

Run `pnpm generate` to regenerate the TypeScript API client
in `website/src/api` based on the OpenAPI specification.
